# Test-only dependencies — not needed to run the app, only to run pytest.
-r requirements.txt
pytest==9.1.1

# Self-provisions real ephemeral Postgres/Redis containers for the test
# session when DATABASE_URL/REDIS_URL aren't already set in the
# environment — see tests/conftest.py. Pinned, not left floating.
testcontainers[postgres,redis]==4.15.0

# Security scanning (P2.5) — run in CI, see .github/workflows/test.yml
bandit==1.9.4
pip-audit==2.10.1
slither-analyzer==0.11.6

# Static analysis (P2.5/audit follow-up) — see .github/workflows/test.yml
# and ruff.toml for what's actually gated vs informational.
ruff==0.16.3
mypy==2.3.1

# Real in-process SMTP server for notifications/ tests (Phase 3) — this
# repo's own testing philosophy is "verify against real infrastructure,
# not mocks" (see CLAUDE.md); this is that, for email, the same way
# Testcontainers is that for Postgres/Redis.
aiosmtpd==1.4.6
