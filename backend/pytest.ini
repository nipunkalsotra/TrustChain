[pytest]
pythonpath = .
testpaths = tests
markers =
    unit: pure logic/function tests, no real DB/chain/network dependency (see tests/conftest.py's auto-classifier)
    integration: tests that touch a real Postgres/Redis/Anvil/subprocess/HTTP dependency
