{
  "name": "trustchain",
  "private": true,
  "description": "Release tooling for the TrustChain monorepo (contracts/, backend/, frontend/, sdk/) — see docs/release-process.md. Not a publishable package itself.",
  "devDependencies": {
    "semantic-release": "^24.2.3",
    "@semantic-release/changelog": "^7.0.0",
    "@semantic-release/git": "^10.0.1",
    "conventional-changelog-conventionalcommits": "^10.4.0"
  }
}
