import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Agents — TrustChain",
}

// Intentionally empty for now — see app/dashboard/page.tsx's comment.
export default function DashboardAgentsPage() {
  return null
}
