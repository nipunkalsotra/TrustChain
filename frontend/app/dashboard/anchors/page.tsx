import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Anchors — TrustChain",
}

// Intentionally empty for now — see app/dashboard/page.tsx's comment.
export default function DashboardAnchorsPage() {
  return null
}
