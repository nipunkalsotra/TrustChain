import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Settings — TrustChain",
}

// Intentionally empty for now — see app/dashboard/page.tsx's comment.
export default function DashboardSettingsPage() {
  return null
}
